import React from "react";
import TitleWindow from "../ui/TitleWindow";

const Instrucctions = () => {
  return (
    <>
      <TitleWindow
        title="Instrucciones"
        descriptionPage="En este apartado encontraras las instrucciones a detalle del proceso de tu servicio social y una guia detalla del(os) sistema(s) que utilizaras a lo largo de."
      />
    </>
  );
};

export default Instrucctions;
